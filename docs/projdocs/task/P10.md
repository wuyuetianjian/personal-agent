# P10 Core Release Slice Completed

## Status

P10 proactive trigger/event core slice completed locally on 2026-09-06.

## Completed

- Added `docs/p10_core_release_slice.md` as the scoped P10 design and acceptance document.
- Added `internal/trigger` with trigger types, validation, in-memory registry, SQLite store, trigger state store, schedule calculation, deterministic execution keys, debounce, cooldown, jitter, and exponential backoff helpers.
- Added `internal/event` with untrusted event envelopes, validation, deterministic dedup keys, an in-memory publish/subscribe bus, SQLite persistence, duplicate event detection, and project event listing.
- Added SQLite migration `0003_p10_core.sql` for `triggers`, `trigger_state`, and `events`.
- Added tests for trigger validation, enabled filtering, one-shot semantics, interval fixed-delay scheduling, simple cron next-fire calculation, noise controls, trigger persistence, event bus persistence, event deduplication, and migration coverage through existing storage tests.
- Updated English and Chinese README sections.

## Validation

```sh
go test ./...
make build
make smoke
```

All three commands passed for this increment.

## Boundaries / Next Steps

- P10 is not fully complete. This archive covers the first foundation slice, not all P10-01 through P10-70 release gates.
- Next recommended work is P10 trigger CLI/API or a proactive runtime worker that reads `internal/trigger.Store`, computes due triggers, and creates P9 persistent workflows.
- Filesystem watchers, condition evaluators, notifications, goals, dead-letter queues, rate governance, startup recovery, and release gates remain follow-up P10 waves.
- Event payloads remain untrusted observations. Future workflow creation must still pass through project policy, capability resolution, permission, privacy, budget, evidence, and verification layers.
- A pre-existing local edit remains in `configs/config.example.yaml` enabling the Codex backend and direct writes. It was not reverted.
